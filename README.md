# FileEncryption
This is a package which includes an encryption script using xor encryption and the decryption script.

run encrypt.py to choose the file you wish to encrypt, then you will create an encryption key and the encrypted file
run decrypt.py to take the encrypted file and the key to produce the encrypted message
